apiVersion: 1

datasources:
  - name: Loki
    uid: loki                # MUST match dashboard JSON references; without this Grafana assigns a random UID
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
      timeout: 60
